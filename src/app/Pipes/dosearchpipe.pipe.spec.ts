import { DosearchpipePipe } from './dosearchpipe.pipe';

describe('DosearchpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DosearchpipePipe();
    expect(pipe).toBeTruthy();
  });
});
